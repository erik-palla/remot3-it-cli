import { expect } from 'chai';
import {
  authorization
} from '../src';

describe('Remot3.it CLI', () => {
  describe('authorization', () => {
    before(() => {

    })
    it('', async () => {

      try {

      } catch (error) {
        expect
      }
    });
  })

})
